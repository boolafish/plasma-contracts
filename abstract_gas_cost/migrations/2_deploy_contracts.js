const App = artifacts.require("App");
const Framework = artifacts.require("Framework");

module.exports = async (deployer) => {
  //await deployer.deploy(Framework);
  //await deployer.deploy(App, Framework.address);
};